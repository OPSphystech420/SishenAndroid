#pragma once

#include "Includes.h"

namespace Off
{
    inline uint32 ViewProjectionMatrix = 0;
    inline uint32 ComponentToWorld = 0;
    inline uint32 ComponentSpaceTransformsArray = 0;
    inline uint32 Bounds = 0;

    namespace Idx
    {
        inline int32 ProcessEvent = 69;
        inline int32 GetAdjustedAim = 0;
    }
}
